import ViewInitProgress from "@/components/client/documents/progresses/init.progress";

const InitProgress = () => {
  return (
    <div>
      <ViewInitProgress />
    </div>
  );
};

export default InitProgress;
